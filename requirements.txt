Python Thinker App Requirements
Core dependencies for the application

Standard library modules used:
- tkinter (usually included with Python)
- json (standard library)
- os (standard library)
- datetime (standard library)
- typing (standard library - Python 3.5+)
- dataclasses (standard library - Python 3.7+)
- uuid (standard library)

No external dependencies required!
This app uses only Python standard library modules.

Optional enhancements you could install:
tkinter-tooltip>=1.0.0  # For enhanced tooltips
pillow>=8.0.0          # For image support in GUI
markdown>=3.0.0        # For enhanced markdown export
